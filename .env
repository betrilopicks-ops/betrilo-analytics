# Disable source maps in production builds.
# CRA defaults GENERATE_SOURCEMAP=true; .map files add ~2 MB per build.
# No error-reporting service consumes them. Saves ~600 MB/month at 300+ deploys.
GENERATE_SOURCEMAP=false
