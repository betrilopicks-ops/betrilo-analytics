import React from 'react';
import PageStub from '../components/PageStub';

export default function EdgeReportPage() {
  return <PageStub title="Edge Report" blurb="Model edges with EV%, sortable by direction and confidence. The sharpest value the model finds each day, published free." />;
}
